import Button from "./ui/button";

const Counter = () => {
    return (
        <Button variant="link" className="bg-primary/50">
            count is
        </Button>
    );
};

export default Counter;
